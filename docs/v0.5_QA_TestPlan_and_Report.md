# 《倒水乐乐乐》v0.5 QA 测试顺序与测试报告

> **适用版本**: v0.5（`GAME_CONFIG.VERSION`: **0.5.0**）  
> **引擎**: Cocos Creator 3.8.8  
> **目标**: 验证 **留存运营系统**（每日签到正式版、成就、主题商店、每日挑战）+ **广告 Mock / 真实模式** + **存档迁移与云合并** + **v0.4/v0.3 关键回归**。  
> **需求对齐**: `docs/v0.5_Feature_Spec.md`，数值与条目细节见 `assets/Scripts/Data/V05Config.ts`。  

---

## 1. 测试前准备（一次性）

### 1.1 环境信息记录

- **测试日期**：________  
- **测试人**：________  
- **测试平台**（勾选）：  
  - [ ] Cocos Creator 编辑器预览  
  - [ ] 微信开发者工具  
  - [ ] 微信真机 / 体验版  
- **Creator 版本**：________  
- **代码版本**：`git log -1 --oneline`：________  
- **构建路径**（若测微信包）：________  

### 1.2 广告：Mock 模式 vs 真实流量主

> **默认**：`GAME_CONFIG.AD.MOCK_ENABLED === **true**`（`GameConfig.ts`）。  
> - **Mock 开启**：激励 / 插屏走延时模拟（约 `MOCK_REWARDED_DELAY` 1500ms、`MOCK_INTERSTITIAL_DELAY` 500ms），Toast 含「模拟广告」类提示；**冷却与每日上限仍生效**。  
> - **Mock 关闭**：需有效 **`AD_UNIT_IDS`**（非占位）且通常需已开通 **微信公众平台 → 流量主**（UV 等条件以**后台实时页面**为准）。  
> **上线前切换生产**：`MOCK_ENABLED = false` + 替换三类广告位 ID。  

### 1.3 云开发与存档

- [ ] `wx.cloud.init` 正常，`kv_saves` 等集合与权限符合策略  
- [ ] **旧存档兼容**：从 v0.4 及更早存档升级后，`unlockedThemes`（现为 **string[]**）、`achievements`、`dailyChallenge` 等字段深合并无报错  

### 1.4 测试前置

- 需要纯净进度时：**设置 → GM → 重置存档**  
- **首日适龄提示**：首次启动会弹出适龄面板；关闭后主菜单 **0.5s** 后会尝试 **自动弹出签到**（若当日未签且弹层为空）。若适龄面板仍占位，`tryShowDailyCheckInOnLaunch` 可能延后——可在关掉适龄后手动点「签到」验收。  
- 打开 Console，关注 `[AdManager]`、`[CloudSaveManager]`、`[WXAPI]`  

---

## 2. 统一记录规则

- 结论：**PASS / FAIL / BLOCKED**  
- **FAIL**：步骤 + 实际表现 + 日志/截图  
- **BLOCKED**：Mock 与真实环境混淆、广告位未开通、关卡资源缺失等  

---

## 3. 测试顺序总览

1. **Smoke**：启动、主菜单文案、最短通关（主线）  
2. **每日签到**：自动弹出、签到、连续加成、补签、断签  
3. **成就**：列表、进度、领取、横幅队列  
4. **主题商店**：解锁、切换、背景生效  
5. **每日挑战**：今日关卡、首通奖励、再挑战、倒计时  
6. **广告**：Mock 激励/插屏/Banner 行为；可选关闭 Mock 后的真实广告回归  
7. **云存与合并**：含 v0.5 新增字段  
8. **回归**：分享、设置、GM、排行榜占位  

---

## 4. Smoke 与版本标识

### S01 启动与主菜单

- **期望**：  
  - 标题 **「倒水乐乐乐」**；副标题 **「把同色液体倒在一起」**  
  - 底部版本 **「v0.5 留存运营版」**  
  - 设置内 **`版本 0.5.0`**（`GAME_CONFIG.VERSION`）  
- **结果**：________  
- **证据**：________  

### S02 主流程最短通关（非每日挑战）

- **步骤**：开始游戏 → 进入当前进度关 → 通关  
- **期望**：正常结算；有 **双倍奖励 / 下一关 / 分享 / 返回选关**；调 `StorageManager.completeLevel` 后统计与主线进度增加  
- **结果**：________  
- **证据**：________  

### S03 每日挑战与主线差异（重要）

- **说明**：`dailyChallengeMode === true` 时，`completeLevel` 走 **`completeDailyChallenge`**，**不**调用 `StorageManager.completeLevel`。  
- **期望（与实现一致）**：本局 **不** 增加 `completedLevels` / 累计通关数等主线统计；**「累计通关」类成就** 不在每日挑战单局内增加。若策划要求「挑战也算一关通关」，则记 **需求/实现差异**。  
- **结果**：________  
- **证据**：________  

---

## 5. 每日签到（DailyCheckIn v2）

### D01 启动自动弹出

- **前置**：当日未签到；`lastCheckInDate` 非今天（或新号）  
- **步骤**：冷启动 → 若首登先关适龄 → 约 **0.5s** 后是否出现签到面板  
- **期望**：未签时自动弹出；已签则不再自动弹（可手点「签到」查看）  
- **结果**：________  

### D02 立即签到

- **步骤**：点 **立即签到**  
- **期望**：`lastCheckInDate` 为今日；`consecutiveDays` +1；`checkInHistory` 对应天置 true；Toast **「签到成功：…」**；奖励为 **第 N 天格子** 对应 `DAILY_CHECKIN_REWARDS`（见 `V05Config`）  
- **结果**：________  

### D03 连续加成

- **规则（代码）**：`consecutiveDays` 与 **`getCheckInMultiplier`**：≥14 天 **×2**；≥7 天且小于 14 **×1.5**；否则 **×1**；**金币** 类乘倍，**钻石** 类按配置原值（以实机逻辑为准）  
- **结果**：________  

### D04 断签（normalizeCheckInBreak）

- **步骤**：将系统日期或存档 `lastCheckInDate` 改为「非昨天非今天」→ 再开签到  
- **期望**：`consecutiveDays` 置 0；`checkInHistory` 全 false（与 `normalizeCheckInBreak` 一致）  
- **结果**：________  

### D05 补签昨天（激励 `check_in_makeup`）

- **步骤**：点 **看视频补签昨天**（Mock 下为模拟视频）  
- **期望**：成功则 `makeupCountThisWeek` 增加，Toast **「补签成功，连续签到已延续」**；**同一自然周第 3 次** 应提示 **「本周补签次数已用完」**（周上限 **2**）  
- **结果**：________  

---

## 6. 成就系统

> 共 **10** 条，配置见 `V05Config.ACHIEVEMENT_CONFIGS`（`complete_10` … `speed_run`）。  

### A01 列表与进度

- **步骤**：主菜单 **成就**  
- **期望**：10 行可辨；显示进度 `current/target`；未达成 / 可领取 / 已领取 状态可区分  
- **结果**：________  

### A02 触发与同步（抽样）

| 成就 ID | 建议验证方式（与实现相关） |
|---------|----------------------------|
| `complete_10/100/500` | 主线通关累加 `statistics.totalLevelsCompleted` 后，弹窗内同步为 max |
| `three_stars_100` | 统计 `levelStars` 中 **≥3 星** 的关卡数 |
| `no_hint_50` | 本关未用提示且主线通关时 +1 |
| `undo_master` | 每次撤销 +1 |
| `ad_watcher_50` | `statistics.totalAdsWatched`（Mock 真看一次涨 1） |
| `share_20` | `social.totalShares` |
| `daily_challenge_7` | 每日挑战 **连续完成天数** `dailyChallenge.consecutiveDays`（非自然日签到的连续） |
| `speed_run` | 单局从进关到通关 **≤30s**（`Date.now() - levelStartTime`） |

- **结果**：________  

### A03 解锁横幅与队列

- **步骤**：制造新解锁（如改存档或快速撤销）  
- **期望**：顶部成就横幅队列播放，不永久卡死 UI  
- **结果**：________  

### A04 领取奖励

- **步骤**：对已 **completed** 且未 **claimed** 的项点领取  
- **期望**：金币/钻石入账；含 **`share_20`** 时可能解锁主题 **sakura**；`claimed` 后按钮状态正确  
- **结果**：________  

---

## 7. 主题商店

> 主题表：`THEME_CONFIGS`（default、candy、ocean、autumn、neon、sakura、vip）。  

### T01 展示与预览

- **步骤**：主菜单 **商店**  
- **期望**：卡片与预览色块正确；**vip** 说明为观看激励 **50** 次等条件  
- **结果**：________  

### T02 解锁与支付

- **步骤**：满足 **通关数 / 分享数 / 广告次数 / 成就** 后，用 **金币或钻石** 购买（以卡片逻辑为准）  
- **期望**：扣费正确，`unlockedThemes` 含该 id；不足时 Toast **金币不足** 等  
- **结果**：________  

### T03 切换与全局背景

- **步骤**：已解锁主题点 **使用**  
- **期望**：`currentTheme` 更新；返回主菜单 / 选关 / 进关，**`getCurrentThemeBackground`** 与瓶子配色来源一致（chapter.theme 与主题背景的优先级以代码为准）  
- **结果**：________  

---

## 8. 每日挑战（DailyChallenge）

### C01 今日关卡与确定性

- **规则**：`getDailyChallengeLevelId(dateKey)` 用日期字符串哈希，`levelId` 范围 **1～maxBundledLevelId**（当前逻辑里 `maxBundledLevelId` 来自关卡资源，默认上限参考 **150**）  
- **步骤**：记录两天或两台设备同一日期，是否 **同一 levelId**  
- **结果**：________  

### C02 面板信息

- **期望**：显示 **今日关卡 N**、**个人最佳步数** 或「今日还没有完成记录」、**刷新倒计时**（`X小时Y分`）  
- **结果**：________  

### C03 首通奖励

- **步骤**：当日 **第一次** 完成挑战（在最佳步数逻辑下首次记入 `completedDates`）  
- **期望**：金币 **+100**；文案 **「每日挑战奖励：金币 +100」**；`daily_challenge_7` 用的连续天在 **`lastChallengeDate` 为昨日** 时 +1，否则从 1 起算  
- **结果**：________  

### C04 再挑战

- **步骤**：首通后再次通关  
- **期望**：文案可为 **「今日奖励已领取，可继续刷新最佳步数」**；步数更优时更新 `bestStepsByDate`  
- **结果**：________  

### C05 与全服榜

- **期望**：当前实现为 **个人最佳 + 今日关卡**，**无** 全服前 100 排行（与 `v0.5_Feature_Spec` 方案 C 一致）；勿按文档旧稿误报 FAIL。  
- **结果**：________  

---

## 9. 广告（Mock 为主，真实可选）

### M01 Mock 激励（含补签场景）

- **步骤**：任意激励入口（提示/撤销/双倍/免费金币/**补签**）  
- **期望**：约 **1.5s** 延迟；Toast **模拟广告**；`rewardedToday`、`totalAdsWatched` 增加；达日上限 / 冷却时提示与 v0.4 一致  
- **结果**：________  

### M02 Mock 插屏与 Banner

- **步骤**：满足插屏关卡条件点 **下一关** / **返回选关**；各界面观察 Banner  
- **期望**：Mock 插屏短延迟且计数；主菜单/选关/结算 Banner 行为与 v0.4 一致（略）  
- **结果**：________  

### M03 关闭 Mock 后的真实广告（可选 / BLOCKED）

- 设 **`MOCK_ENABLED = false`** 且配置真实 **AD_UNIT_IDS** 且流量主已开  
- **期望**：真实素材；失败时不阻断主线  
- **结果**：________  

---

## 10. 云存档与合并（v0.5 字段）

- **步骤**：A 端玩出主题/成就/挑战记录 → 同步 → B 端登录合并  
- **期望**：`CloudSaveManager.mergeSaveData` 对 **`unlockedThemes`、`dailyChallenge.bestStepsByDate`** 等按实现合并（取优/并集），无报错  
- **结果**：________  

---

## 11. 回归与合规（抽样）

### R01 分享 / 登录 / 设置 / GM

- 主菜单 **分享求助**、设置 **同步**、**GM** 摘要含新字段（若已实现）  

### R02 排行榜

- 仍为 **占位 / mock** 则标 **已知限制**，不测真实好友榜  

### R03 无内购入口

- 设置内 **无** 人民币直购 / 去广告付费（与 v0.4 一致）  

---

## 12. 测试结果汇总（测试完成后填写）

### 12.1 总体结论

- **结论**：通过 / 不通过 / 有条件通过  
- **条件说明**：________  

### 12.2 阻塞项（BLOCKED）

- _________  

### 12.3 缺陷清单（可复制追加）

| ID | 严重程度 | 用例 | 简述 | 状态 |
|----|----------|------|------|------|
| BUG-V05-001 | P0/P1/P2 | D__ | | 打开/修复中/关闭 |

### 12.4 发布与广告策略备忘

- **Mock 是否关闭**：________  
- **流量主是否已开通**：________  
- **对外说明**：是否写明 Mock / 广告上线节奏：________  

---

## 13. 修订记录

| 日期 | 修订内容 |
|------|----------|
| 2026-05-06 | 初版：依据 `GameManager` / `V05Config` / `AdManager` / `GameData` 整理 v0.5 测试用例 |
